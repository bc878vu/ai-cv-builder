# Gemini AI — keep the real key out of Git
GEMINI_API_KEY=

# Admin authentication — keep these server-only
ADMIN_PASSWORD=replace-with-a-strong-admin-password
ADMIN_SESSION_SECRET=replace-with-a-long-random-session-secret

# Supabase server-side admin CRUD — NEVER expose the service role key to the browser
SUPABASE_URL=https://bewxefxjbvbumajflhfw.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-your-supabase-service-role-key
